1. Install venv support (one time)
sudo apt update
sudo apt install python3-venv python3-full python3-opencv -y

2. Create virtual environment
python3 -m venv torch_env --system-site-packages

3. Activate virtual environment
source torch_env/bin/activate

(You’ll see (torch_env) in the prompt)

4. Install required libraries (inside venv)
pip install --upgrade pip
pip install torch torchvision numpy

5. Run your program (inside venv)
python infer_pydnet48_esp32cam.py

6. Deactivate venv (optional)
deactivate